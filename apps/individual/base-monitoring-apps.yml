apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: base-monitoring-apps
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/wannabecoder11/k8s-homelab.git
    targetRevision: HEAD
    path: base/monitoring
  destination:
    server: https://kubernetes.default.svc
    namespace: monitoring # ← CHANGE if base/ deploys to specific namespace
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
